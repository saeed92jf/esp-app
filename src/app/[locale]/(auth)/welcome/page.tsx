/* src/[locale]/(auth)/welcome/page.tsx */

// src/app/[locale]/(auth)/welcome/page.tsx
// Route pages MUST default-export a component
export default function WelcomePage() {
  return <div>Welcome (Work In Progress)</div>;
}
